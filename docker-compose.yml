services:
  backend:
    build: ./backend
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "${PORT:-3000}:3000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
